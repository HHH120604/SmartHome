# 环境变量配置文件示例, 请填写对应内容后将本文件更名为.env
DASHSCOPE_API_KEY = "" # 大模型模型api_key
DATABASE_URL = "sqlite:///./data/hongmeng.db" # 数据库地址

# 安全配置
SECRET_KEY = ""
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 60 * 24 * 7

# MQTT配置
MQTT_BROKER_HOST = "localhost"  # MQTT地址
MQTT_BROKER_PORT = 1883 # MQTT端口
MQTT_USERNAME = "" # MQTT用户名（可选）
MQTT_PASSWORD = "" # MQTT密码（可选）

# 应用配置
DEBUG = True
HOST = "0.0.0.0"
PORT = 8000

# 项目信息
PROJECT_NAME = "鸿蒙智能家居API"
VERSION = "1.0.0"
DESCRIPTION = "基于FastAPI的智能家居后端系统"